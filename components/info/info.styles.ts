import { StyleSheet } from 'react-native';

const infoStyles = StyleSheet.create({
  title: {
    fontWeight: 'bold',
    marginBottom: 10,
    color: '#333',
  },
  paragraph: {
    marginBottom: 10,
    color: '#666',
    lineHeight: 22,
  },
});

export default infoStyles;
